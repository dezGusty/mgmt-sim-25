import { UnassignedUsersPipe } from './unassigned-users-pipe';

describe('UnassignedUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new UnassignedUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
